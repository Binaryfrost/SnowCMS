services:
  db:
    image: mysql:8.0.40
    volumes:
      - ./data:/var/lib/mysql
    environment:
      MYSQL_RANDOM_ROOT_PASSWORD: yes
      MYSQL_DATABASE: snowcms
      MYSQL_USER: snowcms
      MYSQL_PASSWORD: snowcms
    ports:
      - 3306:3306